name: Ping Fly.io 

on:
  schedule:
    - cron: '*/30 * * * * *' # Schedule the workflow to run every 30 seconds
jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Send Ping Request
        run: curl -sS -X GET https://blabber-hive.fly.dev

      - name: Check Response
        run: echo "Response received"
